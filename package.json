{
  "private": true,
  "scripts": {
    "biome-check": "biome check --verbose",
    "biome-ci": "biome ci",
    "jest": "jest --config=test/jest.config.js",
    "jest:coverage": "jest --config=test/jest.config.js --coverage",
    "build": "node build.js"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "esbuild": "^0.25.2",
    "jest": "^29.7.0"
  }
}
